import 'package:flutter/material.dart';

class DisplayHousehold extends StatelessWidget {
  const DisplayHousehold({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('household');
  }
}
